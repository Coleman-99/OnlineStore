/*this function is supposed to change css class names on html elemennts
when the page changes to device screen*/

const deviceWidth = window.innerWidth;
document.getElementById("deviceWidth").innerHTML = deviceWidth;
